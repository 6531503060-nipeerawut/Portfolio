# Makes npm actually enforce the "engines" field rather than treat it as advice.
# Node below 20.9 then fails at install time, where the message is about Node,
# instead of somewhere inside the build, where it is about something else.
engine-strict=true
